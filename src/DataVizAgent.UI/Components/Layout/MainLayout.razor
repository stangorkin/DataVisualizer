@inherits LayoutComponentBase

@* The desktop shell (WPF title bar + File menu) provides the app chrome; this layout only
   supplies the full-height flex container the panels stretch into. *@
<div class="app-shell">
    <div class="app-body">
        @Body
    </div>
</div>
